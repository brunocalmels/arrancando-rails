<%= form_with(model: categoria_poi, local: true) do |form| %>
  <% if categoria_poi.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(categoria_poi.errors.count, "error") %> impidió que este Categoría de PoI se guardara:</h2>

      <ul>
        <% categoria_poi.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="actions">
    <%= form.submit :Guardar, class: "btn btn-success" %>
  </div>
<% end %>
