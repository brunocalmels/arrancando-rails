<% provide(:title, "Categorías Pois") %>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @categoria_pois.each do |categoria_poi| %>
      <tr>
        <td><%= link_to 'Ver', categoria_poi %></td>
        <td><%=  link_to 'Editar', edit_categoria_poi_path(categoria_poi) %></td>
        <td><%= link_to 'Destroy', categoria_poi, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Categoria Poi', new_categoria_poi_path %>
