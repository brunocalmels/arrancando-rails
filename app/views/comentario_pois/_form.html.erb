<%= form_with(model: comentario_poi, local: true) do |form| %>
  <% if comentario_poi.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(comentario_poi.errors.count, "error") %> prohibited this comentario_poi from being saved:</h2>

      <ul>
        <% comentario_poi.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :poi_id %>
    <%= form.text_field :poi_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :mensaje %>
    <%= form.text_area :mensaje %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
