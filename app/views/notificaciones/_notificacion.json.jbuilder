json.extract! notificacion,
              :id,
              :titulo,
              :cuerpo,
              :url,
              :leido,
              :user_id,
              :created_at,
              :updated_at

# json.url notificacion_url(notificacion, format: :json)
