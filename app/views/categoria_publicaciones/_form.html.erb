<%= form_with(model: categoria_publicacion, local: true) do |form| %>
  <% if categoria_publicacion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(categoria_publicacion.errors.count, "error") %> impidió que este Caterogía de Publicaciones se guardara:</h2>

      <ul>
        <% categoria_publicacion.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="actions">
    <%= form.submit :Guardar, class: "btn btn-success" %>
  </div>
<% end %>
